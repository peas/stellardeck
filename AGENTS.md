# Agent instructions

Before working in this directory or its descendants:

1. Read `~/.claude/CLAUDE.md` if it exists for shared preferences and cross-project context.
2. Read and follow the `CLAUDE.md` in this directory as the source of truth for project instructions.
3. Read `CLAUDE.local.md` if present, and any more specific `CLAUDE.md` files in subdirectories relevant to the task. Apply more specific instructions within their scope.

Keep project instructions in `CLAUDE.md`; do not duplicate them here.
