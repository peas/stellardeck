// Shim — moved to packages/core/src/print-mode.js (2026-05-04, core extraction).
// Browser-only module; this Node shim is just a placeholder so any tooling
// that resolves the old path doesn't 404. Deleted in step 6.
module.exports = require('./packages/core/src/print-mode.js');
